CREATE TABLE cacophony_comments (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  text TEXT NOT NULL,
  post_id INTEGER REFERENCES cacophony_posts(id) ON DELETE CASCADE NOT NULL,
  user_id INTEGER REFERENCES cacophony_users(id) ON DELETE CASCADE NOT NULL,
  date_created TIMESTAMPTZ DEFAULT now() NOT NULL
);